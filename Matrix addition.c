#include<stdio.h>
int main(){
int rows cols;
printf("enter the number of rows and columns,");
scan f ("%d,"frows,&cols);
inta[rows][cols],b[rows][cols]sum[rows][cols];
print("enter elements of materix a:/n");
for (int i=0i<rows;i++){
for (intj=0,i<cols;j++){
scanf("%d,"&a[i][j]);
 }
}
printf("enter element of mateix b:\n");
for (int i=0:i<rows;i++){
printf("%d",sum[i][j]);
}
