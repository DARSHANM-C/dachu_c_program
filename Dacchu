include <stdio.h>

int main(void) {
  flot p, r, t, si;
  printf("Enter principal rate and time");
  scanf("%f%f%f", &p, &r, &t);
  si = (p * r * t) / 100;
  printf("The simple interest is %f", si);
  printf(darshanmc\n");

  return 0;
}
